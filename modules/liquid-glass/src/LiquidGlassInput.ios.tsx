import { requireNativeViewManager } from "expo-modules-core";

const NativeLiquidGlassInputView = requireNativeViewManager(
  "LiquidGlass",
  "LiquidGlassInputView"
);

export default NativeLiquidGlassInputView;
