import { requireNativeViewManager } from "expo-modules-core";

const NativeLiquidGlassPopoverView = requireNativeViewManager(
  "LiquidGlass",
  "LiquidGlassPopoverView",
);

export default NativeLiquidGlassPopoverView;
